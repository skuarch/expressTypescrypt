{
  "name": "expresstypescrypt",
  "version": "1.0.0",
  "description": "",
  "main": "dist/http-server/start-server.js",
  "scripts": {
    "build": "rm -rf dist/* && tsc",
    "prestart": "npm run build && npm run tslint ",
    "start": "node .",
    "dev": "NODE_ENV=development && npm run nodemon",
    "nodemon": "nodemon",
    "tslint": "tslint --config ./tslint.json --project ./tsconfig.json --force --format stylish"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/express": "^4.17.7",
    "@types/log4js": "^2.3.5",
    "@types/node": "^14.0.27",
    "express": "^4.18.2",
    "log4js": "^6.3.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.4",
    "tslint": "^6.1.2",
    "typescript": "^3.9.7"
  }
}
